export enum Events {
  'USER_CREATED' = 'user.create',
  'OTP_VERIFIED' = 'otp.verified',
  'SEND_EMAIL' = 'send.email',
  'USER_LOGGED_IN' = 'user.loggedin',
  'NEW_RIDER_CREATED' = 'new.rider.created',
  'NEW_VENDOR_CREATED' = 'new.vendor.created',
}

export enum NODE_ENVS {
  'PROD' = 'production',
  'DEV' = 'development',
  'STAGING' = 'staging',
}
